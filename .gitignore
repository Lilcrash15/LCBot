config.json
chatbot.db
chatbot.db-wal
chatbot.db-shm
song_overlay_state.json
lcbot.log*
__pycache__/
*.pyc

# PyInstaller build output -- rebuilt from source by build_exe.bat,
# no reason to version the generated exe/intermediate files.
build/
dist/

# Downloaded/cached at runtime, not source.
emote_cache/

# Local editor/OS cruft.
.vscode/
.idea/
Thumbs.db
.DS_Store

# Build/release helper scripts -- kept out of the public repo on
# purpose (2026-09-26): GitHub should just have the source and the
# exe download, not Ryan's own build/publish tooling. Still used
# locally to actually build and ship releases -- just not tracked.
build_and_release.bat
build_exe.bat
debug_build.bat
fix_push.bat
push_update.bat
release.bat
setup_github.bat
